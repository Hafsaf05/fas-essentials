services:
  store:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      NODE_ENV: production
      PORT: 3001
      DATABASE_PATH: /app/data/store.sqlite
    ports:
      - "127.0.0.1:3001:3001"
    volumes:
      - store-data:/app/data
volumes:
  store-data:
